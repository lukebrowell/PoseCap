interface:
  display_name: ad-next
  short_description: State-aware navigation aid (flutter doctor pattern). Surveys five-layer artifact stack + recommends prioritized next actions. Read-only.
policy:
  allow_implicit_invocation: false

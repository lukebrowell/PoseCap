interface:
  display_name: ad-review
  short_description: Single-session two-axis code review (Codex variant of WORKFLOW §10). Writes audit trail to `.agentic/reviews/`, reports findings inline by default, and ships a bundled `fresh-context-reviewer` subagent for explicit escalation.
policy:
  allow_implicit_invocation: true
